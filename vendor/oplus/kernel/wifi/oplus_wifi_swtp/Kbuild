# SPDX-License-Identifier: GPL-2.0-only
ifeq ($(DEVICE_MODULES_PATH),)
DEVICE_MODULES_PATH = $(srctree)
endif

ccflags-y += -I$(DEVICE_MODULES_PATH)/drivers/misc/mediatek/eccci/port/
obj-$(CONFIG_OPLUS_WIFI_SWTP) := oplus_wifi_swtp.o

