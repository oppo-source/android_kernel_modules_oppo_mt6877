#
# Makefile for the touchscreen drivers.
#

# Each configuration option enables a list of files.

obj-$(CONFIG_TOUCHPANEL_CHIPONE_ICNL9916) += ICNL9916/

oplus_bsp_tp_chipone_common-y += chipone_common.o
obj-$(CONFIG_TOUCHPANEL_CHIPONE) += oplus_bsp_tp_chipone_common.o
#endif
